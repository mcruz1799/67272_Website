<h4>Jobs</h4>
<% if @jobs.empty? %>
  <h5>No Jobs in the system at this time</h5>
<% else %>
  <div class="card">
    <div class="card-content">
      <table>
        <thead>
          <th>Name</th>
          <th>Description</th>
          <th>Active</th>
        </thead>

        <tbody>
          <% @jobs.each do |job| %>
            <tr>
              <td><%= job.name %></td>
              <td><%= job.description unless job.description.nil? %></td>
              <td><%= @job.active ? "Yes" : "No" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
<% end %>